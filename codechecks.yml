checks:
  - name: build-size-watcher
    options:
      files:
        - path: lib/**/*.*
  - name: commit-deployment
  - name: type-coverage-watcher
    options:
      ignoreCatch: true
      ignoreFiles:
        - lib/**/*
      strict: true
